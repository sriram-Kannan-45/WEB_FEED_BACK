// Lightweight toast helper (stub)
export const useToast = () => {
  const toast = (message: string) => {
    // In a real app, replace with a toast library
    console.log('Toast:', message);
  };
  return { toast };
};
